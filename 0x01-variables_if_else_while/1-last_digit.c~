#include <stdlib.h>

#include <time.h>

/* more headers goes there */



/* betty style doc for function main goes there */

int main(void)
  
{
  
  int n, l;
  

  
  srand(time(0));
  
  n = rand() - RAND_MAX / 2;
  l = n % 10;
  /* your code goes there */
  /* checkes the condition of 3 boolean values */
  printf("%d is the last digit\n", l);XS
    return (0);
  
}
